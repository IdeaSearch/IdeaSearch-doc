---
title: Overview
description: IdeaSearch 文档概览
---

import { getPageTreePeers } from "fumadocs-core/server";
import { source } from "@/lib/source";

<Cards>
  {getPageTreePeers(source.pageTree, "/docs/framework").map((peer) => (
    <Card key={peer.url} title={peer.name} href={peer.url}>
      {peer.description}
    </Card>
  ))}
</Cards>
